services:
  chat-mirror:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chat-mirror
    ports:
      - "8080:8080"
    environment:
      - REDACT_LOGS=true
    command: python app.py